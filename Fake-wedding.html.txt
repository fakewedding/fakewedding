<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fake Wedding - Paolo & Fantasia</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Georgia', serif; line-height: 1.6; color: #333; background: linear-gradient(to bottom, #f9e4e4, #fff); overflow-x: hidden; }
        .section { min-height: 100vh; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; padding: 20px; animation: fadeIn 1s ease-in; position: relative; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(50px); } to { opacity: 1; transform: translateY(0); } }
        h1, h2 { font-size: 2.5em; color: #d4a5a5; margin-bottom: 20px; text-shadow: 2px 2px 4px rgba(0,0,0,0.1); }
        @media (max-width: 768px) { h1, h2 { font-size: 2em; } }
        p { font-size: 1.2em; max-width: 600px; margin: 0 auto 20px; }
        .hearts { font-size: 3em; color: #ff69b4; animation: pulse 2s infinite; }
        @keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.1); } 100% { transform: scale(1); } }
        form { background: rgba(255,255,255,0.9); padding: 30px; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); max-width: 400px; width: 100%; }
        input, select, button { width: 100%; padding: 15px; margin: 10px 0; border: 1px solid #d4a5a5; border-radius: 10px; font-size: 1em; }
        button { background: #ff69b4; color: white; border: none; cursor: pointer; font-weight: bold; transition: background 0.3s; }
        button:hover { background: #e55a9b; }
        #countdown { font-size: 1.5em; color: #ff69b4; margin: 20px 0; }
        .gallery { display: flex; flex-wrap: wrap; justify-content: center; gap: 20px; }
        .gallery img { width: 150px; height: 150px; object-fit: cover; border-radius: 10px; box-shadow: 0 5px 15px rgba(0,0,0,0.2); }
        nav { position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%); background: rgba(255,255,255,0.9); padding: 10px; border-radius: 50px; display: flex; gap: 10px; box-shadow: 0 5px 20px rgba(0,0,0,0.1); z-index: 10; }
        nav a { color: #d4a5a5; text-decoration: none; padding: 10px 20px; border-radius: 20px; transition: background 0.3s; }
        nav a:hover { background: #d4a5a5; color: white; }
        #hero-video { position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; z-index: -1; opacity: 0.7; }
        .music-toggle { position: fixed; top: 20px; right: 20px; background: #ff69b4; color: white; border: none; padding: 10px; border-radius: 50%; cursor: pointer; z-index: 10; }
    </style>
</head>
<body>
    <button class="music-toggle" onclick="toggleMusic()">🎵</button>
    
    <nav>
        <a href="#home">Home</a>
        <a href="#info">Info</a>
        <a href="#story">Storia</a>
        <a href="#rsvp">RSVP</a>
    </nav>

    <section id="home" class="section">
        <video id="hero-video" autoplay muted loop>
            <source src="WhatsApp-Video-2026-03-17-at-18.55.55.mp4" type="video/mp4">
            <!-- O embed YouTube: <iframe src="https://www.youtube.com/embed/ID_VIDEO" frameborder="0" allowfullscreen></iframe> -->
        </video>
        <div class="hearts">💕</div>
        <h1>Fake Wedding</h1>
        <h2>Paolo & la Sposa Immaginaria</h2>
        <p>Ispirato al tuo video: un evento FAKE epico con musica, risate e zero serietà! Siete invitati.</p>
        <div id="countdown"></div>
    </section>

    <section id="info" class="section">
        <h2>📅 Dettagli Evento</h2>
        <p><strong>Data:</strong> 15 Giugno 2026, ore 18:00</p>
        <p><strong>Luogo:</strong> Spiaggia di Senigallia, Marche, Italia</p>
        <p>Dress code: Fantasia totale! Portate solo il divertimento (e magari un costume fake).</p>
    </section>

    <section id="story" class="section">
        <h2>💑 La Nostra Storia Finta (dal Video)</h2>
        <p>Come nel tuo video musicale, tutto inizia con una proposta surreale. Ora rendiamolo realtà... finta!</p>
        <div class="gallery">
            <img src="https://via.placeholder.com/150?text=Promo+1" alt="Promo 1">
            <img src="https://via.placeholder.com/150?text=Promo+2" alt="Promo 2">
            <img src="https://via.placeholder.com/150?text=Promo+3" alt="Promo 3">
        </div>
    </section>

    <section id="rsvp" class="section">
        <h2>🎉 Prenota Ora!</h2>
        <form id="rsvpForm">
            <input type="text" placeholder="Nome completo" required>
            <select required>
                <option value="">Ospiti totali</option>
                <option value="1">1 persona</option>
                <option value="2+">2 o più</option>
            </select>
            <input type="email" placeholder="Email" required>
            <button type="submit">Conferma Partecipazione Fake!</button>
        </form>
    </section>

    <audio id="bgMusic" loop>
        <!-- Aggiungi src="tua-musica.mp3" qui, ispirata al video -->
    </audio>

    <script>
        // Countdown
        const eventDate = new Date('2026-06-15T18:00:00').getTime();
        const countdownEl = document.getElementById('countdown');
        setInterval(() => {
            const now = new Date().getTime();
            const diff = eventDate - now;
            const days = Math.floor(diff / (1000 * 60 * 60 * 24));
            countdownEl.innerHTML = `Fake Day in ${days} giorni! ⏳💍`;
        }, 1000);

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', e => {
                e.preventDefault();
                document.querySelector(anchor.getAttribute('href')).scrollIntoView({ behavior: 'smooth' });
            });
        });

        // RSVP
        document.getElementById('rsvpForm').addEventListener('submit', e => {
            e.preventDefault();
            alert('RSVP FAKE confermato! Preparati al caos divertente. 📧');
        });

        // Toggle musica (aggiungi il tuo file audio)
        function toggleMusic() {
            const music = document.getElementById('bgMusic');
            if (music.paused) {
                music.play();
            } else {
                music.pause();
            }
        }

        // Play video on load
        window.onload = () => {
            document.getElementById('hero-video').play();
        };
    </script>
</body>
</html>